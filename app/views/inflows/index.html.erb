<% content_for :page_title, "Entradas" %>
<div class="space-y-6 animate-fade-in">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-3xl font-bold text-white">Entradas</h1>
      <p class="mt-1 text-gray-400">Gerencie as entradas de produtos no estoque</p>
    </div>
    <%= link_to new_inflow_path, class: "inline-flex items-center px-6 py-3 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-medium rounded-lg shadow-lg shadow-green-500/30 transition-all duration-300 hover:scale-105" do %>
      <i class="bi bi-plus-circle mr-2 text-lg"></i>
      Nova Entrada
    <% end %>
  </div>

  <div class="bg-gray-800/50 backdrop-blur-xl rounded-xl border border-gray-700/50 p-6">
    <%= form_with url: inflows_path, method: :get, class: "flex gap-3" do %>
      <div class="flex-1">
        <%= text_field_tag :query, params[:query], placeholder: "Buscar por produto, fornecedor...", class: "w-full px-4 py-3 bg-gray-900/50 border border-gray-700 rounded-lg text-gray-100 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all" %>
      </div>
      <%= submit_tag "Buscar", class: "px-6 py-3 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-medium rounded-lg transition-all" %>
    <% end %>
  </div>

  <div class="bg-gray-800/50 backdrop-blur-xl rounded-xl border border-gray-700/50 overflow-hidden">
    <div class="overflow-x-auto">
      <table class="w-full">
        <thead>
          <tr class="bg-gradient-to-r from-green-600/20 to-emerald-600/20 border-b border-gray-700/50">
            <th class="px-6 py-4 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">ID</th>
            <th class="px-6 py-4 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">Produto</th>
            <th class="px-6 py-4 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">Fornecedor</th>
            <th class="px-6 py-4 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">Quantidade</th>
            <th class="px-6 py-4 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">Data</th>
            <th class="px-6 py-4 text-left text-xs font-medium text-gray-300 uppercase tracking-wider">Descrição</th>
            <th class="px-6 py-4 text-center text-xs font-medium text-gray-300 uppercase tracking-wider">Ações</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-700/50">
          <% @inflows.each do |inflow| %>
            <tr class="hover:bg-gray-700/30 transition-colors">
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">#<%= inflow.id %></td>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-white"><%= inflow.product.title %></td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300"><%= inflow.supplier.name %></td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300"><span class="px-3 py-1 bg-green-600/20 text-green-400 rounded-full">+<%= inflow.quantity %></span></td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300"><%= inflow.created_at.strftime('%d/%m/%y') %></td>
              <td class="px-6 py-4 text-sm text-gray-400 max-w-xs truncate"><%= inflow.description || '-' %></td>
              <td class="px-6 py-4 whitespace-nowrap text-center text-sm">
                <%= link_to inflow_path(inflow), class: "inline-flex items-center px-3 py-2 bg-blue-600/20 hover:bg-blue-600/40 text-blue-400 rounded-lg transition-colors" do %>
                  <i class="bi bi-eye"></i>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <% if @inflows.empty? %>
      <div class="text-center py-12">
        <i class="bi bi-inbox text-6xl text-gray-600"></i>
        <p class="mt-4 text-gray-400">Nenhuma entrada encontrada</p>
      </div>
    <% end %>
  </div>

  <div class="flex justify-center">
    <%== pagy_tailwind_nav(@pagy) %>
  </div>
</div>
